# PlasmaNetworkV26
cd velocity && java -jar server.jar
-----------------------------------
cd limbo && java -jar server.jar

cd server && java -jar server.jar

cd UHCs && java -jar server.jar

cd bedwars && java -jar server.jar

25567 public

./autosave.sh

